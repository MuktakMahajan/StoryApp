type Query {
  getUserDetails(id: ID!): [User!]!
}

type Mutation {
  addProfilePicture(id: ID!, photo: String!): [User!]!
}

type User {
  id:ID!
  name: String!
  description: String
  photo: String
}